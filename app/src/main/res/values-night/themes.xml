<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark variant. Deep blue grey window background. -->
    <style name="Theme.FlareEnough" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/deep_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>
